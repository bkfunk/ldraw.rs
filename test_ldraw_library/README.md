This test ldraw library includes a handful of different parts that can be used
for testing. It is generated by first identifying the parts to be included,
copying those files from a complete LDraw download, and then resolving the
dependency tree and copying the subparts and primitives used by the target
parts.

The directory structure maches the structure of the complete LDraw library.
